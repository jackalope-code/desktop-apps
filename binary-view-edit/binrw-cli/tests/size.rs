#[cfg(test)]
mod size_tests {
    use super::*;

    #[test]
    fn check_size_command() {
        assert_eq!(4, 4);
    }
    
    #[test]
    fn check_large_file_size() {
        assert_eq!(4, 4);
    }
}